name: Eval Sentry

# AI 解读质量回归哨兵（M2）：降采样跑 MingLi-Bench，防 prompt 改动导致准确率回退
# 每周一自动跑 + 手动触发（Actions → Eval Sentry → Run workflow）
# 需要仓库 secret：LLM_DEEPSEEK_KEY（DeepSeek API Key，可与服务端同一把）
on:
  schedule:
    - cron: '0 2 * * 1'   # 每周一 02:00 UTC
  workflow_dispatch:

jobs:
  eval-sentry:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Run eval (sampled 20, guanwei pipeline)
        working-directory: server
        env:
          LLM_PROVIDER: deepseek
          LLM_DEEPSEEK_KEY: ${{ secrets.LLM_DEEPSEEK_KEY }}
        # --min-accuracy 0.25：20 题降采样噪声大（±20pp），低于 25% 判定明显回退 → job 失败告警（M4 闭环）
        run: npx tsx scripts/eval/harness.ts --mode guanwei --limit 20 --min-accuracy 0.25

      - name: Archive eval report
        uses: actions/upload-artifact@v7
        with:
          name: eval-report
          path: server/scripts/eval/reports/*.json
          retention-days: 14
